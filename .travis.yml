---
sudo: false
language: java

cache:
  directories:
    - $HOME/.m2

jdk:
  - oraclejdk8

install: true
script:
  - cd WeatherClient && mvn clean install
  - bash <(curl -s https://codecov.io/bash)

branches:
  only:
    - master
